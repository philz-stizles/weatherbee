import { render, screen } from '@testing-library/react';
import Notes from './Notes';

describe('Notes', () => {
  test('placeholder', () => {
    const placeholder = 'Notes places...';
    // render(<Notes />);
  });
});
