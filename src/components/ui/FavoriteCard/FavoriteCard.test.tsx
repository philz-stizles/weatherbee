import { render } from '@testing-library/react';
import FavoriteCard from './FavoriteCard';

describe('FavoriteCard', () => {
  test('renders', () => {
    const data = {};
    // render(<FavoriteCard data={data} />);
  });
});