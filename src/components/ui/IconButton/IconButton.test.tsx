import { render } from '@testing-library/react';
import IconButton from './IconButton'

describe('IconButton', () => {
  test('renders', () => {
    const data = {};
    // render(<IconButton icon={} />);
  });
});
